 <body>
    <section class="overlay">
      <h1>Guess the right number!</h1>
      <form action="check.html" method="GET">
        <label for="guess">Enter a number between 1-10:</label>
        <br>
        <input type="number" name="guess" id="guess" min="1" max="10" required> <!--required, means a value must be entered-->
        <br>
        <input type="submit" id="submission" value="Submit">
      </form>
      <form action="submission.html" method="GET">
        <label for="username">Username:</label>
        <br>
        <input id="username" name="username" type="text" minlength="3" maxlength="15" required pattern="[a-zA-Z0-9]+"> 
        <!--characters must be 3 or above and 15 or below-->
        <!--Any other symbol will make the username invalid-->
        <br>
        <label for="pw">Password:</label>
        <br>
        <input id="pw" name="pw" type="password" minlength="8" maxlength="15" required> <!--Characters must be 8 or above and 15 or below-->
        <br>
        <input type="submit" value="Submit">
      </form>
    </section>
  </body>
</html>
